import React from "react";
import Autocom from "../components/Autocom";

const Test = props => {
  return (
    <div>
      <div className="container">
        <Autocom />
      </div>
    </div>
  );
};

export default Test;
